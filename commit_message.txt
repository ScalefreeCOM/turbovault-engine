feat(cli): consolidate commands and add debug mode

* feat(cli): add --type flag for export type selection
  - Replace --json-only and --export-json with single --type flag
  - Support 'dbt' and 'json' export types
  - Add interactive type selection when flag not provided

* feat(cli): add debug mode with --debug flag
  - Add global --debug/-d flag for detailed execution logging
  - Create debug utility module with conditional logging
  - Add debug output throughout CLI execution flow
  - Support TURBOVAULT_DEBUG environment variable

* fix(cli): resolve command execution hanging issues
  - Remove problematic MigrationExecutor checks causing hangs
  - Skip migration checks for non-init commands
  - Simplify ensure_database_ready() to avoid blocking operations
  - Commands now execute properly without hanging

* feat(cli): consolidate run command into generate
  - Remove redundant 'turbovault run' command
  - Integrate JSON export functionality into generate command
  - Update all documentation to reflect new command structure

* fix(cli): improve serve command stability
  - Add proper error handling for server process
  - Add debug logging for server startup
  - Improve exit code handling

* docs: update CLI documentation
  - Update CLI_GUIDE.md with --type flag examples
  - Update README.md with new command structure
  - Remove references to deprecated --json-only and --export-json flags

BREAKING CHANGE: The 'turbovault run' command has been removed. Use 'turbovault generate --type json' instead.
BREAKING CHANGE: The --json-only and --export-json flags have been replaced with --type flag.
